<Window x:Class="ChemicalApp.View.AddDebet"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChemicalApp.View"
        xmlns:local1="clr-namespace:ChemicalApp"
        mc:Ignorable="d"
        Title="AddDebet" Height="450" Width="800"
        DataContext="{Binding MainWindow, Source={StaticResource Locator}}">
    <DockPanel>
        <StackPanel DockPanel.Dock="Bottom">
            <Button Content="Ok" Command="{Binding SaveDebet}"/>
        </StackPanel>
        <Grid DataContext="{Binding CurrentDebet}">
        <StackPanel Orientation="Vertical">
            <TextBlock Text="Id"/>
            <TextBlock Text="{Binding ProductId}"/>
            <TextBlock Text="Дата"/>
            <DatePicker SelectedDate="{Binding Date}"/>
            <TextBlock Text="Количество"/>
            <TextBox Text="{Binding Sum}"/>
        </StackPanel>
    </Grid>
    </DockPanel>
</Window>
